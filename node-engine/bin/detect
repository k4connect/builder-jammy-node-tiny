#!/usr/bin/env bash
set -euo pipefail
APP_DIR="${CNB_APP_DIR:-/workspace}"

if [[ -f "${APP_DIR}/package.json" ]]; then
  cat <<'TOML'
[[provides]]
name = "node"
[[provides]]
name = "npm"
[[provides]]
name = "node_modules"
TOML
  exit 0
fi

exit 100
